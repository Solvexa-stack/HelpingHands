// W3 UI isolation: the Organization Workspace shares this feature page
// with the platform — the workspace shell (layout, nav, guards) differs.
export { default } from '../../(dashboard)/projects/page';
